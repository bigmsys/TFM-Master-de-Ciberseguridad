apt update
apt install python3 python3-pip python3-venv libmysqlclient-dev pkg-config python3-dev default-libmysqlclient-dev build-essential -y

mkdir -p ~/python/auth/{acl,user,superuser}
cd python/
python3 -m venv .venv
source .venv/bin/activate
pip install flask mysqlclient pyotp bcrypt

vim app.py
chmod u+x app.py

vim config.ini
chmod 600 config.ini

./app.py
